import { defineConfig } from 'vitepress'

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "vitepress-reproduce-reactive-class",
  description: "vitepress-reproduce-reactive-class"
})
